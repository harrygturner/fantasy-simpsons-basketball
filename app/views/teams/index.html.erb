<div class="mx-auto" style="background-color: white; text-align:center; padding:10px; width:50%">
<% @teams.each do |team| %>
<% if team.user != @user %>
    <p><span class="teamdisplay">Team Name</span>: <%= team.name %> | <span class="teamdisplay">Owner</span>: <%= team.user.name %> 
    
      <%= form_tag("/game", method: :post) do%>
      <%= hidden_field_tag :away_team, team.id %>
        <%= submit_tag "Setup Game!", class:"btn btn-outline-info" %>
      <%end%>
    <% end %>
  
  
  </p>
<% end %>
</div>